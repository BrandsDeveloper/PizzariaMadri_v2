<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.PizzariaMadri"
        tools:targetApi="31">
        <activity
            android:name=".numero_do_pedido"
            android:exported="false" />
        <activity
            android:name=".envioPedido"
            android:exported="false" />
        <activity
            android:name=".Initial"
            android:exported="false"
            android:label="@string/title_activity_main2" /> <!-- Tela Inicial -->
        <activity
            android:name=".termos"
            android:exported="true" /> <!-- Tela de Termos -->
        <!-- Tela Conta -->
        <activity
            android:name=".Politicas"
            android:exported="true" /> <!-- Tela de Politicas -->
        <activity
            android:name=".version"
            android:exported="true" /> <!-- Tela versão -->
        <activity
            android:name=".Pizza"
            android:exported="true" /> <!-- Tela de Pizza -->
        <activity
            android:name=".Login"
            android:exported="true"
            android:screenOrientation="fullSensor" /> <!-- Tela de Login -->
        <activity
            android:name=".cadastro"
            android:exported="true"
            android:screenOrientation="fullSensor" /> <!-- Tela de Cadastro -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="fullSensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Tela de carregamento -->
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>